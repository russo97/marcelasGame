
const tilesCount = state => state.tiles.length;

const userSequenceCount = state => state.userSequence.length;

const computedSequenceCount = state => state.computedSequence.length;

export default {
  tilesCount,
  userSequenceCount,
  computedSequenceCount
}
